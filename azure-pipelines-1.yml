trigger:
- master

pool:
  name: Default
  demands:
    - agent.name -equals SAJJAD

stages:
- stage: Build2
  displayName: 'Build Stage'
  jobs:
  - job: BuildJob2
    displayName: 'Build Job'
    steps:
    - script: |
        echo "Restoring project dependencies..."
      displayName: 'Restore dependencies'
    - script: |
        echo "Running unit tests..."
      displayName: 'Run unit tests'